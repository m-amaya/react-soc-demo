import { Flex } from '@chakra-ui/react';
import styled from '@emotion/styled';

export const ProjectFieldContainer = styled(Flex)`
  align-items: center;
  border-radius: 6px;
  border-style: solid;
  border-width: 1px;
  gap: 6px;
  margin: 0 8px;
  padding: 8px 12px;
`;
