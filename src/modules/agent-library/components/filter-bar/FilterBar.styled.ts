import { Flex } from '@chakra-ui/react';
import styled from '@emotion/styled';

export const FilterBarRoot = styled(Flex)`
  justify-content: space-between;
`;

export const ToggleWrapper = styled(Flex)`
  flex-direction: row;
  gap: 8px;
`;
