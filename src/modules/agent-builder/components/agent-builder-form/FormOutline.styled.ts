import { Flex } from '@chakra-ui/react';
import styled from '@emotion/styled';

export const FormOutlineRoot = styled(Flex)`
  flex: 1;
  flex-direction: column;
  gap: 2px;
  max-width: 180px;
  padding: 8px;
`;
